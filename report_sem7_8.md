# Семинар 7-8. Расчет метрик объектно-ориентированного проектирования

## 1. Цель работы

Цель работы - рассчитать метрики объектно-ориентированного проектирования для проекта по предметной области «Музей», вариант 23. Расчет нужен, чтобы оценить наследование, связность классов, инкапсуляцию, полиморфизм и распределение ответственности между классами.

## 2. Задание по ТЗ

По ТЗ семинара 7-8 требуется выполнить расчет метрик для своей предметной области.

Базовые метрики по классам:

- `WMC` - количество методов класса;
- `DIT` - глубина дерева наследования;
- `NOC` - количество непосредственных потомков;
- `CBO` - количество связей класса с другими классами;
- `RFC` - количество методов, которые могут быть вызваны в ответ на сообщение объекту;
- `LCOM` - оценка несвязности методов класса.

Системные метрики MOOD:

- `MIF` - фактор наследования методов;
- `AHF` - фактор скрытия атрибутов;
- `POF` - фактор полиморфизма;
- `COF` - фактор сцепления классов;
- `MHF` - фактор скрытия методов;
- `CHF` - фактор скрытия классов.

Расчет выполнен вручную по исходному коду проекта. Стандартные библиотечные классы C++ в метриках связности не учитывались.

## 3. Описание предметной области

Предметная область проекта - музей. В системе есть музей, залы, каталог, музейные объекты, экспонаты разных типов, гид, посетитель и очередь отложенных вызовов из предыдущего семинара.

Центральный класс `Museum` связывает залы, каталог и коллекцию экспонатов. Экспонаты хранят номер зала, поэтому зал логически группирует экспонаты, а сама коллекция объектов хранится в `Museum`. Класс `Catalog` отвечает за поиск экспонатов. `Guide` хранит маршрут экскурсии, а `Visitor` демонстрирует связь посетителя с гидом.

Очередь `PendingQueue`, интерфейс `IPending` и шаблон `Pending` относятся к семинару 6, но тоже входят в текущий проект и используются для демонстрации шаблонов, полиморфизма и отложенного выполнения методов.

## 4. Список анализируемых классов

| Класс | Назначение |
|---|---|
| `MuseumItem` | Абстрактный базовый класс музейного объекта. |
| `Exhibit` | Общий класс экспоната, наследуется от `MuseumItem`. |
| `Painting` | Картина, конкретный тип экспоната. |
| `Sculpture` | Скульптура, конкретный тип экспоната. |
| `Artifact` | Артефакт, конкретный тип экспоната. |
| `Hall` | Зал музея: хранит номер и название зала. |
| `Museum` | Центральный класс: хранит залы, коллекцию экспонатов и каталог. |
| `Catalog` | Выполняет поиск экспоната по названию. |
| `Guide` | Хранит имя гида и маршрут из музейных объектов. |
| `Visitor` | Посетитель, который может быть связан с гидом. |
| `MyContainer` | Собственный шаблонный контейнер проекта. |
| `IPending` | Интерфейс отложенного вызова. |
| `Pending` | Шаблонная реализация отложенного вызова. |
| `PendingQueue` | Очередь отложенных вызовов. |

## 5. Схема наследования

Основная иерархия предметной области:

```text
MuseumItem
`-- Exhibit
    |-- Painting
    |-- Sculpture
    `-- Artifact
```

Иерархия очереди отложенных вызовов:

```text
IPending
`-- Pending
```

В иерархии `MuseumItem` используются виртуальные методы:

- `printInfo()`;
- `getRecommendedVisitMinutes()`.

Классы `Painting`, `Sculpture` и `Artifact` переопределяют эти методы. Это показывает полиморфизм: коллекция музея хранит объекты через `std::shared_ptr<MuseumItem>`, но при выводе информации вызывается метод конкретного типа экспоната.

## 6. Таблица метрик по классам

В таблице используются следующие обозначения:

- `Methods` - все методы класса;
- `Attrs` - все атрибуты класса;
- `PubM` - публичные методы;
- `HidM` - закрытые или защищенные методы;
- `PubA` - публичные атрибуты;
- `HidA` - закрытые или защищенные атрибуты;
- `Overrides` - количество переопределенных виртуальных методов.

| Класс | Methods | Attrs | PubM | HidM | PubA | HidA | DIT | NOC | CBO | RFC | LCOM | Overrides | Комментарий |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---|
| `MuseumItem` | 6 | 2 | 6 | 0 | 0 | 2 | 0 | 1 | 1 | 6 | 0.0 | 0 | Абстрактный базовый класс музейного объекта. |
| `Exhibit` | 3 | 2 | 3 | 0 | 0 | 2 | 1 | 3 | 1 | 7 | 0.0 | 0 | Общий класс экспоната. |
| `Painting` | 4 | 1 | 4 | 0 | 0 | 1 | 2 | 0 | 1 | 9 | 0.0 | 2 | Переопределяет поведение музейного объекта. |
| `Sculpture` | 4 | 1 | 4 | 0 | 0 | 1 | 2 | 0 | 1 | 9 | 0.0 | 2 | Переопределяет поведение музейного объекта. |
| `Artifact` | 4 | 1 | 4 | 0 | 0 | 1 | 2 | 0 | 1 | 9 | 0.0 | 2 | Переопределяет поведение музейного объекта. |
| `Hall` | 5 | 2 | 5 | 0 | 0 | 2 | 0 | 0 | 1 | 5 | 0.0 | 0 | Зал музея, связан с `Museum`. |
| `Museum` | 15 | 4 | 15 | 0 | 0 | 4 | 0 | 0 | 4 | 24 | 1.0 | 0 | Центральный класс системы. |
| `Catalog` | 1 | 0 | 1 | 0 | 0 | 0 | 0 | 0 | 2 | 4 | 0.0 | 0 | Поиск экспонатов в контейнере. |
| `Guide` | 5 | 2 | 5 | 0 | 0 | 2 | 0 | 0 | 2 | 9 | 0.0 | 0 | Гид хранит маршрут из `MuseumItem*`. |
| `Visitor` | 3 | 2 | 3 | 0 | 0 | 2 | 0 | 0 | 1 | 4 | 0.0 | 0 | Посетитель связан с гидом. |
| `MyContainer` | 23 | 4 | 20 | 3 | 0 | 4 | 0 | 0 | 3 | 28 | 1.0 | 0 | Собственный шаблонный контейнер. |
| `IPending` | 2 | 0 | 2 | 0 | 0 | 0 | 0 | 1 | 1 | 2 | 0.0 | 0 | Интерфейс отложенного вызова. |
| `Pending` | 2 | 2 | 2 | 0 | 0 | 2 | 1 | 0 | 2 | 4 | 0.0 | 1 | Реализация `IPending`. |
| `PendingQueue` | 5 | 1 | 5 | 0 | 0 | 1 | 0 | 0 | 2 | 8 | 0.0 | 0 | Очередь отложенных вызовов. |

Самые большие значения `WMC` имеют `MyContainer` и `Museum`. Это ожидаемо: `MyContainer` реализует много операций контейнера, а `Museum` является центральным классом предметной области. Значение `DIT` не превышает 2, поэтому наследование остается понятным.

## 7. Расчет системных метрик MOOD

Для системных метрик использовались 14 анализируемых классов. Расчет является учебным и выполнен по исходному коду.

| Метрика | Значение | Расчет | Пояснение |
|---|---:|---|---|
| `MIF` | 0.20 | 20 унаследованных методов / 102 доступных метода | Наследование используется в иерархии экспонатов и в `IPending -> Pending`, но большая часть классов не наследуется. |
| `AHF` | 1.00 | 24 скрытых атрибута / 24 атрибута | Все поля классов закрыты через `private` или `protected`, прямого доступа к атрибутам нет. |
| `POF` | 0.09 | 7 переопределений / 82 объявленных метода | Полиморфизм есть, но используется только в нужных местах: экспонаты и отложенные вызовы. |
| `COF` | 0.09 | 17 реальных связей / 182 возможные направленные связи | Связность умеренная. Основные связи сосредоточены вокруг `Museum`, `Guide`, `Catalog`, `MyContainer` и `PendingQueue`. |
| `MHF` | 0.04 | 3 скрытых метода / 82 метода | Скрытые методы есть в `MyContainer`; остальные классы в основном открывают только простой публичный интерфейс. |
| `CHF` | 0.00 | 0 скрытых классов / 14 классов | Все анализируемые классы объявлены в заголовочных файлах и доступны другим частям проекта. |

## 8. Анализ результатов

Архитектура проекта соответствует предметной области «Музей». Центральный класс `Museum` связывает залы, каталог и экспонаты. `Catalog` отвечает за поиск, `Guide` и `Visitor` демонстрируют взаимодействие объектов предметной области, а `PendingQueue` показывает механизм отложенных вызовов из предыдущего семинара.

Иерархия `MuseumItem -> Exhibit -> Painting / Sculpture / Artifact` показывает наследование и полиморфизм. Базовый класс `MuseumItem` задает общий интерфейс, а конкретные типы экспонатов переопределяют вывод информации и расчет рекомендуемого времени осмотра.

Глубина наследования небольшая: максимальный `DIT` равен 2. Это хорошо для учебного проекта, потому что структура легко читается и не создает лишней сложности.

Связность классов умеренная. Наибольшую роль играет `Museum`, так как он хранит коллекцию экспонатов, залы и каталог. Также заметна связность `MyContainer`, потому что контейнер используется несколькими классами проекта.

Инкапсуляция соблюдается: публичных атрибутов нет, поля скрыты через `private` или `protected`. Это подтверждается значением `AHF = 1.00`.

## 9. Рекомендации по рефакторингу

1. Если проект будет расширяться, вынести построение маршрутов из `Museum` в отдельный класс, например `RouteBuilder`.
2. Сделать связь между `Hall` и экспонатами более явной: сейчас экспонат хранит номер зала, а коллекция находится в `Museum`.
3. При росте логики поиска расширить `Catalog`, чтобы он отвечал не только за поиск по названию, но и за фильтрацию по залу, году или автору.
4. Добавить автоматическую утилиту расчета метрик по заголовочным файлам, чтобы не выполнять расчет вручную.
5. Разделить демонстрационный код и предметную логику, если `main.cpp` станет слишком большим.

## 10. Вывод

Проект по предметной области «Музей», вариант 23, подходит для анализа метрик объектно-ориентированного проектирования. В нем есть наследование, полиморфизм, инкапсуляция, композиция, агрегация, собственный шаблонный контейнер и очередь отложенных вызовов.

Расчет метрик показал, что система остается простой и понятной: наследование неглубокое, связность умеренная, атрибуты скрыты, а полиморфизм используется только там, где он действительно нужен.
